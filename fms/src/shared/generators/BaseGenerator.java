package shared.generators;

import java.util.logging.Logger;

import static shared.util.LoggingHelper.setupLogger;

abstract class BaseGenerator {

    protected static final Logger log = setupLogger("fms-generator");

}
